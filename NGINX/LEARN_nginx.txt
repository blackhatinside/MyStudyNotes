nginx
  gateway b/w internet and backend infra.
                  
req --> webserver

visit webapp --> req goes to --> webserver --> webserver looks for requested resources --> sends it back as resp.

  event driven arch.
  used as reverse proxy
  security++
  caching++
  etc/nginx.conf


server distinguished by the port it listens to
acts as a load balancer
send req. to server with fewest active connections; round robin 

nginx:
  HTTP web serving
  HTTP reverse proxy
  HTTP load balancer
  HTTP cache
  proxy
