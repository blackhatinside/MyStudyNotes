# find all processes using port 1234
netstat -ano | findstr :1234

# kill process with process ID
Stop-Process -Id 21792 -Force

# to install 'make' command
choco install make

# 
$env:NODE_OPTIONS="--max-old-space-size=6096"
